<h1>Categorias</h1>
<% @categories.each do |category| %>
 <div class="form-group">
   <h2>
    <%= link_to category.nombre_categoria, admin_category_path(category) %>
   </h2>
     <div class="pull-right">
       <%= link_to "Mostrar", admin_category_path(category),class: "btn btn-primary" %>
       <%= link_to "Editar", edit_admin_category_path(category), class: "btn btn-success" %>
       <%= link_to "Eliminar", admin_category_path(category), class: "btn btn-danger", method: :delete, data: { confirm: 'Estas seguro de eliminar la categoría?' } %>
    </div>
 </div>
 <br>
  <hr/>
<% end %>

